# ============================================================================
# ⚠️ 绝对不能加进来的三项（加了就构建失败）：
#   .sqlx/       —— SQLX_OFFLINE=true 时 query! 宏的离线元数据
#   migrations/  —— sqlx::migrate!("./migrations") 是编译期宏，要读目录
#   Cargo.lock   —— cargo build --locked 依赖它
# ============================================================================

/target

.git
.gitignore
.github

.env
.env.*
!.env.example

docs/
# template/ 不忽略：送货单 Excel 模板需经 builder 阶段 COPY 进运行时镜像
# （见 Dockerfile stage 2：DELIVERY_NOTE_TEMPLATE_DIR=/app/template）
tests/
scripts/

*.md
**/*.md

Dockerfile
.dockerignore
docker-compose.yml

.claude/
.idea/
.vscode/
.DS_Store
**/.DS_Store
*.log
**/*.rs.bk
