# 2026-09-20：限制集成测试并发数 + 自定义 runner（修复 ephemeral-postgres 容器泄漏）
#
# runner 接管 PG 容器生命周期（绑到 binary 进程，shell trap EXIT 清理）。
# 详细见 scripts/test_runner.sh 的 4 个转义口 + 主路径设计。
[test]
env = { RUST_TEST_THREADS = "4" }

[target.'cfg(unix)']
runner = "scripts/test_runner.sh"