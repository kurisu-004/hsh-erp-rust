-- ============================================================================
--  assembly 域集成测试 fixture (PR13 Phase H, 2026-09-24)
--
--  加载入口：test-support::fixture::load_assembly_fixture(pool)
--  加载方式：内部先 load_part_fixture() 复用 part 域基线（ID 段 10-49），
--           再 raw_sql(include_str!("../../fixtures/assembly.sql")) 加载
--           本文件（ID 段 70+，仅 1 行 t_serial_counter）。
--
--  ## 设计原则
--  - 所有 ID 走常量 9_000_000_000_000_000_070+ 区段（process_chain 1-9，
--    part 10-49，delivery 50-52，production 60-69，物理不相交）。
--  - 审计字段 created_at / updated_at 用 now()。
--  - 不引 trigger / 不调 hsh_erp_rust 函数 —— SQL 仅 INSERT 静态行。
--  - 无新用户：复用 part 域 MANAGER 用户 fx_part_manager（密码 "changeme"，
--    bcrypt cost=12 哈希已在 part.sql 内预生成）。
--
--  ## 不预置 t_customer（L1 / L2）
--  t_customer.serial_prefix 上有 `uq_t_customer_root_prefix` 唯一索引
--  (parent_id IS NULL AND deleted_at IS NULL AND serial_prefix IS NOT NULL)，
--  全局活跃根客户的 prefix 必须唯一。assembly 测试每用例都动态插自己的 L1
--  （prefix='F' 18/20 + prefix='X' 1/20 + List 1/20），与预置 L1 同 prefix
--  会撞唯一约束。fixture 不预置 t_customer，测试保留本地
--  `insert_l1_customer` / `insert_l2_customer` helper 按需造不同 prefix / name。
--
--  ## 不预置 t_assembly / t_part / t_part_batch
--  assembly 域测试走 service 层（`AssemblyService::create_assembly`）直调，
--  按需造不同 drawing_no / customer_id / quantity / 带不带 PDF / 不同
--  child count。状态机不允许从 IN_PROCESS / COMPLETED 等回退 PENDING，
--  预置会污染「期望空库」断言。
--
--  ## t_serial_counter 行（1 行）
--  (prefix='F', counter=0) —— 18/20 assembly 测试用 'F' prefix；
--  余下 'X' prefix 测试（list_with_filters_and_l1_expansion）本地保留
--  `insert_serial_counter(pool, "X", 0)`（ON CONFLICT DO UPDATE 复用，
--  不与预置行冲突）。
-- ============================================================================

-- ---- 序列号计数器（canonical 'F' prefix）----
INSERT INTO t_serial_counter (prefix, counter, version, created_at, updated_at)
VALUES ('F', 0, 0, now(), now());